__pycache__/ 
*.pyc 
*.db
*.db-shm
*.db-wal 
.env
cache/
logs/ 
.streamlit/secrets.toml 
data_pipeline_log*.txt
data_pipeline/xbrl/temp/
data_pipeline/xbrl/raw_cache/
data/yf_cache/
data_pipeline/nse_prices/raw/
# Local canary run artifact — CI uploads it separately
canary_report.json

# Canary harness ephemeral outputs (committed snapshots are kept under scripts/snapshots/)
canary_report.json
canary_report.md

# Windows shortcuts
*.lnk
token.txt

# Parquet dual-write output — files can be 100+ MB, rebuilt by
# export_to_parquet.py / CI. Sync to S3 or git-lfs for persistence.
data/parquet/
.env.local

# Env backups / leftovers — never commit. The .bak variant is
# especially dangerous because people create it right before
# rotating keys and then forget to rotate.
.env*.bak
.env.*.bak
*.env.bak
.env.backup

# Local Python venv
.venv/

# Ad-hoc debug artifacts from agent runs
parity_scorecard.json
tmp_screener.html
preview.html
scripts/keep_awake.ps1
.claude/worktrees/

# Debug dumps / agent artifacts (2026-04-25)
_backfill*.log
_full*.log
dcf_full.txt
scripts/data_patches/_backfill_pe_pb_progress.json
scripts/data_patches/_backfill_pe_pb_untouchable.json
sector_isolation_report.*
canary_artifacts/
dcf_artifacts/
scripts/snapshots/snapshot_*.json
