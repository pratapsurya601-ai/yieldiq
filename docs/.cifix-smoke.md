# CI Flake Smoke Marker

This file exists only to trigger the CI gates after the
2026-05-23 CI-flake fix trio (PRs #592, #595, #598, #600). It touches
no code path. Safe to delete after the next clean run is observed
on main.

Expected outcomes on this PR:

- Sector Isolation (merge gate) - PASS (no-op): diff touches no
  scoring/data files.
- DCF Regression Tests - skipped (no path match) or PASS against
  the refreshed baseline.
- Canary Diff (merge gate) - skipped (no path match) or PASS in
  under 10 min with parallel fetch.
