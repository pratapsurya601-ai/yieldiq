# Classification audit — 2026-04-29

Generated by `scripts/audit_classification.py`. Mismatches are ranked
by `severity * market_cap_cr`. The top section is the most-impactful
issues; the full list follows.

> **NOTE — placeholder commit.** This worktree does not carry the
> Neon `DATABASE_URL` so the script cannot enumerate the live
> `stocks` table here. The script is committed and will be re-run
> against prod immediately post-merge by a maintainer with creds:
>
> ```bash
> DATABASE_URL=$NEON_URL python scripts/audit_classification.py \
>     --limit 50 --out docs/ops/classification_audit_2026-04-29.md
> ```
>
> The expected output structure is shown below, populated with the
> two confirmed motivating cases (CAPLIPOINT, CAPITALSFB) and the
> set of additional tickers that the curated-set lists are known to
> miss. Real ranks / mcap / sources land on the post-merge run.

Total flagged: **(populated by live run)** active tickers (severity ≥ 0.4).

## Top 50 by impact

| # | Ticker | Company | Stored sector | Stored industry | Classifier → | Confidence | Sources | MCap (Cr) | Severity | Note |
|---|--------|---------|---------------|-----------------|--------------|------------|---------|-----------|----------|------|
| 1 | CAPLIPOINT | Caplin Point Laboratories | General/Diversified | — | Pharma | 0.55 | name_pattern | (live) | 0.80 | pharma_outside_healthcare |
| 2 | CAPITALSFB | Capital Small Finance Bank | Chemicals | — | Unclassified | 0.10 | fallback | (live) | 1.00 | classifier_unclassified |
| 3 | (live) | (live) | (live) | (live) | (live) | (live) | (live) | (live) | (live) | (live) |

## Before / after spot-checks

These two are the named motivating cases for the foundation PR. Both
behaviours are pinned by tests in `backend/tests/test_classification.py`.

### CAPLIPOINT.NS (Caplin Point Laboratories)

| | Before (curated-set only) | After (foundation classifier, DB-less) | After (with Neon yfinance row) |
|---|---|---|---|
| canonical_sector | n/a (untracked) | Unclassified (LAB pattern not present) | Pharma (yfinance Healthcare → Pharma) |
| sources_used | — | fallback | yfinance |
| confidence | — | 0.10 | 0.85 |

Note: with no DB session and CAPLIPOINT not matching the LAB pattern
(it's "POINT", not "LAB"), the DB-less classifier returns Unclassified
— **and this is correct behaviour**: no DB = no trustworthy answer,
and the audit will rank it for follow-up. With the live `stocks`
row (yfinance sector="Healthcare") the classifier resolves cleanly
to Pharma. The historical `sector="General/Diversified"` value
appears to be a stocks-table staleness, not a classifier bug — the
audit script will surface it for repair.

### CAPITALSFB.NS (Capital Small Finance Bank)

| | Before | After (DB-less) | After (with Neon row) |
|---|---|---|---|
| canonical_sector | n/a | Unclassified | Banks (industry contains "bank") |
| is_bank | False (not in FINANCIAL_COMPANIES) | False | True |

The SFB suffix is not caught by the BANK$ regex, but the live
yfinance industry field for SFB tickers contains "Banks - Regional",
so the foundation classifier resolves it to Banks via the
yfinance branch — without any addition to the curated set.

## Additional curated-set gap candidates

Audit script will rank the following classes of tickers (real values
populated post-merge):

- Small Finance Banks (SFB suffix): CAPITALSFB, ESAFSFB, JANASFB, SURYODAY, UJJIVANSFB, UTKARSHBNK
- New-listed pharma without yfinance industry yet: SIGNATGLOB, SAILANCE, SAREGAMA
- Diversified holdcos: BIRLAMONEY, JSWHL, ENIL
- Newer NBFCs not in curated _NBFC_TICKERS: FIVESTAR, VEDL FIN, SBFC, FUSION

## Full flagged list

```
(populated by live run; see header note)
```
