[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No health check — Streamlit takes 60+ seconds to start
# and loads data on first request
healthcheckPath = ""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
